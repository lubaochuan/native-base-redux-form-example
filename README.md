Reproduced example from http://docs.nativebase.io/docs/examples/ReduxFormExample.html

Install needed libraries:
```
npm install redux react-redux --save
npm install --save redux-form
npm install native-base --save
```
